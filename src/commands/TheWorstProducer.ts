/*import { Message } from "discord.js";
import { Command, CommandMessage, Description, Guard } from "@typeit/discord";
import { config } from "../config";

export abstract class TheWorstProducer {
  @Command("niklasisannoying")
  @Guard(NotBot)
  async niklasannoyingalert(command: CommandMessage): Promise<void> {
    // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
    // eslint-disable-next-line @typescript-eslint/no-unsafe-member-access
    const sound = command.args.sound;

    // DM Matt, ask him if he wants to either mute, kick or ban
    // If mute, mute for 5m or whatever
    // If kick,
  }
}
*/
